kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  disableDefaultCNI: true  # Disable kindnetd
  podSubnet: 172.16.0.0/16 # Calico's default
nodes:
  - role: control-plane
  - role: worker
  - role: worker

# kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.32.1/manifests/calico.yaml