apiVersion: apps/v1
kind: Deployment
metadata:
  name: canary-test-v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: canary-test-v2
  template:
    metadata:
      labels:
        app: canary-test-v2
    spec:
      containers:
      - name: app
        image: hashicorp/http-echo:1.0
        args:
        - "-text=VERSION 2"
        ports:
        - containerPort: 5678