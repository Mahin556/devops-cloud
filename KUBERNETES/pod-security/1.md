```bash
kubectl apply -f -<<EOF
apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  securityContext:
    runAsUser: 1000
    runAsGroup: 3000
    fsGroup: 2000
    runAsNonRoot: true
  containers:
    - name: app
      image: busybox
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
      volumeMounts:
      - name: demo
        mountPath: /app
  volumes:
  - name: demo
    emptyDir: {}
EOF

kubectl exec -it secure-pod -- sh

kubectl delete pod secure-pod
```
```bash
~ $ mount | grep ' / '
overlay on / type overlay (ro,relatime,lowerdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/176/fs,upperdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/206/fs,workdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/206/work,uuid=on)

kubectl exec -it secure-pod -- id
kubectl exec -it secure-pod -- ls -ld /app /
kubectl exec -it secure-pod -- touch /demo
kubectl exec -it secure-pod -- sudo touch /demo
kubectl exec -it secure-pod -- su -
kubectl exec -it secure-pod -- "mount | grep -e ' / ' -e /app"
```
```bash
kubectl apply -f -<<EOF
apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  securityContext:
    runAsUser: 1000
    runAsGroup: 3000
    fsGroup: 2000
    runAsNonRoot: true
  containers:
    - name: app
      image: busybox
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: false
      volumeMounts:
      - name: demo
        mountPath: /app
  volumes:
  - name: demo
    emptyDir: {}
EOF
```
```bash
~ $ mount | grep ' / '
overlay on / type overlay (rw,relatime,lowerdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/225/fs,upperdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/226/fs,workdir=/var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/226/work,uuid=on)
```