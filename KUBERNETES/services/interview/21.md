## What Happens to Service IP if the Pod is Deleted?

**Answer:** The Service IP **does NOT terminate**. It becomes free/floating.

- If a pod is deleted, the service continues to exist
- The service IP remains available
- When a new pod with matching labels is created, it automatically gets associated
- The service IP is independent of any specific pod
